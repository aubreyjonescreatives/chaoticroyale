import { useEffect, useState } from 'react'
import './GameArea.scss'

// Game Type, Win/Loss

const GameArea = props => {
    const [ gameType, setGameType ] = useState({ game: ''})
    const [ winState, setWinState ] = useState({ win: false })

    
}

export default GameArea